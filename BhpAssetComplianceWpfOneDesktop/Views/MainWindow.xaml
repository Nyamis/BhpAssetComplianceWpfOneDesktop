<Window x:Class="BhpAssetComplianceWpfOneDesktop.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:utility="clr-namespace:BhpAssetComplianceWpfOneDesktop.Utility"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding MyTitle}" Height="390" Width="780">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <ItemsControl ItemsSource="{Binding OurRibbonItems}" Grid.Column="0" ></ItemsControl>

        <ContentControl Grid.Column="1" x:Name="Main" prism:RegionManager.RegionName="{x:Static utility:RegionNames.MainRegion}"/>
    </Grid>
</Window>
